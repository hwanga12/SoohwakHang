# AgriBot 현재 진행 상황 및 명령어 모음

### 다시 빌드 / 실행할 때 명령어

가제보 실행 전 잔여 프로세스 정리:

```bash
cd /home/ssafy/SSAFY/S14P21A602/agribot_ws

# 좀비(Z) 자체는 직접 kill 되지 않으므로, Gazebo/ROS 잔여 프로세스를 먼저 정리
pkill -TERM -f 'gz sim|gzserver|gzclient|gazebo|ign gazebo|rviz2|slam_toolbox|planner_server|controller_server|bt_navigator|lifecycle_manager|map_server|amcl|component_container'
sleep 2
pkill -KILL -f 'gz sim|gzserver|gzclient|gazebo|ign gazebo|rviz2|slam_toolbox|planner_server|controller_server|bt_navigator|lifecycle_manager|map_server|amcl|component_container'

# Z 상태가 남아 있으면 관련 부모 프로세스까지 정리
ps -eo ppid=,stat=,cmd= \
  | awk '$2 ~ /^Z/ && $0 ~ /(agribot|gazebo|gz sim|gzserver|gzclient|rviz2|ros2 launch)/ {print $1}' \
  | sort -u \
  | xargs -r kill -KILL
```

빌드를 한 적이 없는 사람 기준:

```bash
cd /home/ssafy/SSAFY/S14P21A602/agribot_ws
source /opt/ros/jazzy/setup.bash
colcon build --packages-up-to agribot_bringup --symlink-install
source install/setup.bash
```

클린 빌드 기준:

```bash
cd /home/ssafy/SSAFY/S14P21A602/agribot_ws
rm -rf build install log
source /opt/ros/jazzy/setup.bash
colcon build --packages-up-to agribot_bringup --symlink-install
source install/setup.bash
```

- `ros2 launch agribot_bringup simulation.launch.py`를 쓸 때는 `agribot_bringup`까지의 의존 패키지를 함께 빌드해야 한다. `--packages-up-to agribot_bringup`이면 `agribot_interfaces`를 포함한 필요한 패키지가 같이 빌드된다.
- 처음 빌드와 클린 빌드의 차이는 `rm -rf build install log` 유무뿐이다.
- 이전 실패 빌드에서 현재 셸이 이미 꼬여 있다면 새 터미널에서 다시 시작하는 편이 가장 안전하다.

정적 맵 네비게이션:

```bash
ros2 launch agribot_bringup simulation.launch.py
```

SLAM 기반 자율 매핑:

```bash
ros2 launch agribot_navigation autonomous_mapping.launch.py
```

## 2026-03-23 기준

- 기본 자율 매핑 전략은 이제 `mapping_strategy:=sweep_hybrid`다.
- 기본 정적 맵, 수확 patrol metadata, crop catalog의 기준 zone은 이제 `farm_01`이다.
- `sweep_hybrid`의 동작 순서는 아래와 같다.
  - patrol sweep 자동 시작
  - patrol 완료
  - frontier hole-fill 자동 요청
- 기본 목표는 로보락 스타일에 가까운 전진 위주 sweep를 유지하면서, 노트북 발열과 연산량을 크게 늘리지 않는 것이다.
- 이번 기준선의 핵심 변경은 아래다.
  - patrol 주행의 주권 강화
  - non-observe waypoint 연속 구간을 `NavigateThroughPoses`로 배치 주행
  - mapping profile에서 `allow_unknown: true`
  - BT의 180도 recovery spin과 과도한 retry 축소
  - SLAM map update를 `4.0s -> 2.0s`로 낮춰 반응성 회복

## 빌드

```bash
cd /home/ssafy/SSAFY/S14P21A602/agribot_ws
source /opt/ros/jazzy/setup.bash
colcon build --packages-up-to agribot_navigation --symlink-install
source install/setup.bash
```

## 권장 실행 명령

### 1. 기본 실행: sweep_hybrid

직사각형 farm 레인을 patrol로 먼저 쓸고, 완료 후 필요한 경우에만 frontier hole-fill을 요청한다. 현재 권장 기본 실행이다.

```bash
cd /home/ssafy/SSAFY/S14P21A602/agribot_ws
source /opt/ros/jazzy/setup.bash
source install/setup.bash
ros2 launch agribot_navigation autonomous_mapping.launch.py \
  mapping_strategy:=sweep_hybrid \
  use_rviz:=false
```

### 2. patrol만 사용하는 저발열 sweep

frontier hole-fill 없이 순수 patrol sweep만 쓰고 싶을 때 사용한다.

```bash
cd /home/ssafy/SSAFY/S14P21A602/agribot_ws
source /opt/ros/jazzy/setup.bash
source install/setup.bash
ros2 launch agribot_navigation autonomous_mapping.launch.py \
  mapping_strategy:=patrol_only \
  use_rviz:=false
```

### 3. frontier만 사용하는 일반 탐사

자유 공간 중심의 일반 frontier 탐사가 필요할 때만 사용한다.

```bash
cd /home/ssafy/SSAFY/S14P21A602/agribot_ws
source /opt/ros/jazzy/setup.bash
source install/setup.bash
ros2 launch agribot_navigation autonomous_mapping.launch.py \
  mapping_strategy:=frontier_only \
  use_rviz:=false
```

### 4. RViz를 켠 시각화 디버그 실행

```bash
cd /home/ssafy/SSAFY/S14P21A602/agribot_ws
source /opt/ros/jazzy/setup.bash
source install/setup.bash
ros2 launch agribot_navigation autonomous_mapping.launch.py \
  mapping_strategy:=sweep_hybrid \
  use_rviz:=true
```

### 5. SLAM + Nav2만 올리고 매핑 전략은 끄기

실험적으로 상위 전략 없이 SLAM과 Nav2만 띄우고 싶을 때 사용한다.

```bash
cd /home/ssafy/SSAFY/S14P21A602/agribot_ws
source /opt/ros/jazzy/setup.bash
source install/setup.bash
ros2 launch agribot_navigation autonomous_mapping.launch.py \
  mapping_strategy:=patrol_only \
  patrol_autostart:=false \
  use_rviz:=false
```

### 6. 키보드 수동 주행으로 SLAM 매핑하고 저장하기

자율 매핑 대신 사람이 키보드로 로봇을 움직여 지도를 그리고, 완성 후 직접 저장하고 싶을 때 쓰는 방법이다.

터미널 1: 수동 매핑용 SLAM 스택 실행

```bash
cd /home/ssafy/SSAFY/S14P21A602/agribot_ws
source /opt/ros/jazzy/setup.bash
source install/setup.bash
ros2 launch agribot_navigation mapping.launch.py use_rviz:=true
```

- 이 launch는 Gazebo + EKF + `slam_toolbox` + RViz만 올린다.
- 상위 자율 전략(`patrol`, `frontier_explorer`)은 올리지 않으므로, 로봇은 직접 움직여야 한다.

터미널 2: 키보드 teleop 실행

```bash
cd /home/ssafy/SSAFY/S14P21A602/agribot_ws
source /opt/ros/jazzy/setup.bash
source install/setup.bash
ros2 run teleop_twist_keyboard teleop_twist_keyboard --ros-args -r cmd_vel:=/cmd_vel
```

- `spawn_agribot.launch.py` 기준으로 teleop 입력은 `/cmd_vel`로 넣으면 된다.
- watchdog가 중간에 있어 오래된 속도 명령은 자동으로 끊긴다.

수동으로 매핑할 때 권장 순서:

1. 시작점에서 천천히 직진해서 초기 스캔이 안정적으로 쌓이게 한다.
2. 온실 외곽이나 큰 통로 윤곽을 먼저 한 바퀴 훑는다.
3. 각 레인을 끝까지 다녀오며 빈 공간을 메운다.
4. RViz의 `/map`에서 하얀 free space와 벽 윤곽이 거의 다 채워졌을 때 저장한다.

터미널 3: 맵 저장

```bash
cd /home/ssafy/SSAFY/S14P21A602/agribot_ws
source /opt/ros/jazzy/setup.bash
source install/setup.bash
ros2 run nav2_map_server map_saver_cli -f \
  /home/ssafy/SSAFY/S14P21A602/agribot_ws/src/agribot_navigation/maps/farm_map
```

- 저장 결과는 아래 두 파일로 생긴다.
  - `farm_map.yaml`
  - `farm_map.pgm`
- 같은 이름으로 다시 저장하면 기존 파일을 덮어쓴다.

필요하면 즉시 정지:

```bash
ros2 topic pub --once /cmd_vel geometry_msgs/msg/Twist \
  "{linear: {x: 0.0}, angular: {z: 0.0}}"
```

RViz 없이 가볍게 수동 매핑만 돌리고 싶다면:

```bash
cd /home/ssafy/SSAFY/S14P21A602/agribot_ws
source /opt/ros/jazzy/setup.bash
source install/setup.bash
ros2 launch agribot_navigation mapping.launch.py use_rviz:=false
```

## 상태 확인 명령

### patrol 상태

```bash
ros2 topic echo /mapping_patrol/status --once
```

### frontier 상태

```bash
ros2 topic echo /mapping_explorer/status --field data --once --full-length
```

### LiDAR / map 갱신률

```bash
ros2 topic hz /agribot/lidar
ros2 topic hz /map
```

### 매핑 관련 노드

```bash
ros2 node list | rg 'patrol|frontier|slam|controller|planner|collision'
```

### 수확 경로 샘플 확인

새 `farm_01` crop catalog 기준으로 특정 토마토까지의 접근/복귀 계획을 바로 확인할 수 있다.

```bash
cd /home/ssafy/SSAFY/S14P21A602/agribot_ws
source /opt/ros/jazzy/setup.bash
source install/setup.bash
ros2 run agribot_navigation plan_harvest_route --tomato-id farm01_plant_37_tomato_01
```

- 현재 canonical ID 규칙은 `farm01_plant_<nn>` / `farm01_plant_<nn>_tomato_01` 이다.
- 수확 patrol metadata는 `agribot_navigation/config/patrol_waypoints.yaml`, crop catalog는 `agribot_description/config/crop_instances.yaml` 기준으로 계산된다.

## 현재 발열/연산량 해석

- `sweep_hybrid`는 frontier를 기본 주행 엔진으로 쓰지 않는다.
- 그래서 아래 비용이 기본 경로에서 줄어든다.
  - 잦은 frontier goal 재선정
  - 큰 recovery spin
  - repeated bootstrap / boundary-follow decision loop
- 반면 SLAM 반응성을 위해 `map_update_interval`은 `2.0s`로 낮췄다.
- 이 증가분은 patrol-first 배치 주행과 reduced recovery가 상쇄하는 쪽이라, 전체적으로는 큰 발열 증가 없이 더 자연스러운 sweep가 나오도록 설계했다.
- 가장 낮은 발열은 여전히 `use_rviz:=false`일 때다.

## 운영 메모

- `mapping_strategy:=sweep_hybrid`에서는 patrol과 frontier 노드가 함께 올라오지만, frontier는 patrol 완료 전까지 auto-start되지 않는다.
- patrol은 연속 non-observe 구간을 `NavigateThroughPoses`로 묶어 stop-and-go를 줄인다.
- inspect waypoint는 짧은 dwell만 유지한다.
- 매핑용 planner는 `allow_unknown: true`라 초기 미지 공간에서도 patrol path가 더 부드럽게 열린다.
- recovery는 Nav2 BT와 상위 전략 모두에서 짧은 회전 위주로 줄였고, 큰 180도 spin 반복은 피하도록 조정했다.

## RViz 화면 요소 의미

현재 `mapping.rviz` 화면은 여러 display가 겹쳐서 보인다. 헷갈리기 쉬운 요소를 아래처럼 보면 된다.

### 1. 하얀색 바닥

- 주로 `/map`을 그리는 `Map` display다.
- 이미 SLAM이 "여기는 지나갈 수 있는 바닥"이라고 판단한 free space가 밝게 보인다.
- 완전히 빈 배경이 아니라, 로봇이 스캔해서 확보한 2D 지도 영역이라고 보면 된다.

### 2. 회색 격자 바닥

- 실제 지도 데이터가 아니라 `Grid` display다.
- RViz 기준 좌표계를 보기 쉽게 깔아둔 배경선이다.
- 지도나 장애물 의미는 없다.

### 3. 빨간 선

- 문서에서 가장 헷갈리기 쉬운 부분이다.
- RViz를 켜자마자 보이는 빨간색이 항상 `Global Plan`은 아니다.
- 구분은 아래처럼 하면 된다.
  - 얇고 한 줄로 이어진 빨간 선: `Global Plan`일 가능성이 크다.
  - 띠처럼 두껍거나 격자/셀처럼 깔린 빨간 표시: `Costmap`일 가능성이 크다.
- `Global Plan`일 때:
  - 토픽은 `/plan`
  - 전역 planner가 계산한 "멀리 보면 이렇게 가라"는 경로다.
  - 보통 더 길고, 통로 전체를 관통하는 큰 경로처럼 보인다.
- startup 직후 아직 목표 경로가 없는데도 빨간색이 보인다면, 그건 대개 경로선이 아니라 costmap 쪽 표시다.

### 4. 파란 선

- 기본적으로 `Local Plan`이다.
- 토픽은 `/local_plan`이고, controller가 현재 순간에 실제로 따라가려는 짧은 경로다.
- 빨간 전역 경로 위를 완전히 그대로 따르기보다, 현재 로봇 자세와 주변 장애물을 반영해서 더 짧고 자주 바뀐다.

### 5. 로봇 주변에서 움직이는 빨간 점선/붉은 띠처럼 보이는 것

- 대체로 `Local Costmap` 영향이다.
- 로봇 중심으로 함께 움직이는 근거리 회피용 비용 지도이며, 토픽은 `/local_costmap/costmap`이다.
- 의미는 "이 근처는 가까운 장애물 때문에 피해야 하거나, 들어가면 비용이 매우 커지는 구역"이다.
- 실제 바닥에 그려진 선이 아니라, 로봇 주변 안전 판단용 격자창이라고 보면 된다.
- 즉, 물리적 벽 그 자체를 빨간 선으로 직접 그린 것이라기보다, 벽/장애물 때문에 Nav2가 위험하다고 판단한 셀을 붉게 보여주는 것이다.

### 6. 로봇이 지나가는 쪽에 겹쳐 보이는 빨간색/파란색 선

- 빨간색은 경우가 두 가지다.
  - 얇은 선이면 전역 planner의 의도 경로
  - 셀/띠처럼 깔리면 costmap의 고비용 구역
- 파란색은 대체로 local controller의 즉시 추종 경로이거나 costmap 계열 표시다.
- 그래서 로봇이 움직일 때
  - 빨간 얇은 선과 파란 선이 거의 겹치면: 전역/지역 경로가 잘 일치하는 상태
  - 로봇 주변에 빨간 띠가 생기면: 근처 장애물/벽 때문에 local 회피 비용이 커진 상태
- 정리하면
  - 빨간 얇은 선: 어디로 갈지
  - 파란 얇은 선: 지금 당장 어떻게 갈지
  - 빨간 띠/셀: 피해야 할 고비용 구역

### 7. 흰 점이나 밝은 점들

- `LaserScan` display다.
- 토픽은 `/agribot/lidar`이고, LiDAR가 현재 감지한 벽/장애물 반사점이다.
- 로봇 주변에서 실시간으로 반짝이며 바뀌는 점 구름처럼 보인다.

### 8. 로봇 자체

- `RobotModel` display다.
- `/robot_description` 기준으로 URDF/SDF 모델을 RViz에 그린 것이다.
- 실제 주행 판단 데이터라기보다, 현재 TF 위치에 맞춰 로봇 외형을 보여주는 시각화다.

### 9. 참고: 왜 여러 색이 한꺼번에 겹쳐 보이나

- 이 RViz 설정은 아래 display를 동시에 켜 둔 상태다.
  - `Map`
  - `LaserScan`
  - `Global Costmap`
  - `Local Costmap`
  - `Global Plan`
  - `Local Plan`
  - `RobotModel`
  - `TF`
- 그래서 한 지점에 "지도 + costmap + 계획 경로 + 센서 점"이 겹쳐 보일 수 있다.
- 가장 실무적으로 중요한 해석은 아래처럼 구분하는 것이다.
  - 빨간 얇은 선: 전역 경로
  - 파란 얇은 선: 현재 추종 경로
  - 빨간/파란 셀 또는 띠: costmap 시각화
