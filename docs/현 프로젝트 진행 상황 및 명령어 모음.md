## 2026-03-20

### 이번에 진행한 작업

- 현재 AgriBot 저장소를 코드 수정 없이 read-only로 병렬 진단했다.
- 조사 범위는 `ROS 코어/시뮬레이션`, `미구현 런타임`, `서비스 계약/클라이언트 준비`의 3축으로 나눴다.
- 목적은 지금 당장 P0 데모 완주를 막는 지점을 찾고, 구현 우선순위를 다시 고정하는 것이었다.

### 현재 기준선

- `agribot_bringup`, `agribot_description`, `agribot_navigation`, `agribot_interfaces`에는 이미 재사용 가능한 뼈대가 있다.
- 특히 `simulation.launch.py`, `navigation.launch.py`, `mapping.launch.py`, `autonomous_mapping.launch.py`를 중심으로 Gazebo, 정적맵 기반 주행, SLAM 기반 매핑, 순찰/수확 라우팅 자산이 갖춰져 있다.
- `farm_world.sdf`, `greenhouse_map.yaml`, `crop_instances.yaml`, `patrol_waypoints.yaml`, `zones.yaml` 같은 월드/맵/메타데이터 자산도 이미 정리되어 있다.
- `frontier_explorer`, `harvest_routing` 쪽은 테스트 자산까지 있어, 현재 저장소에서 가장 안정적인 축으로 판단했다.
- 반면 `agribot_perception`, `agribot_control`, `agribot_iot`는 패키지 골격만 있고 실제 런타임 노드와 통합 launch가 거의 없다.
- `backend`는 FastAPI 라우트 뼈대는 있지만, 다수 엔드포인트가 placeholder 응답 수준이고 WebSocket도 echo 수준이라 실서비스 계약의 구현체로 보기 어렵다.

### 가장 위험한 갭

- 가장 큰 공백은 `perception -> mission/control -> iot -> mqtt/backend`로 이어지는 실제 런타임 체인이 비어 있다는 점이다.
- ROS 코어도 그대로는 불안정하다. `map -> odom` 중복 발행, LiDAR topic 이름 불일치, 센서 TF 중복, `map/odom` 좌표계 혼용이 남아 있다.
- 그래서 현재 자산만으로는 수동 트리거 데모는 가능해도, 자동 감지 기반 P0 데모는 바로 완주하기 어렵다.
- backend도 문서와 구현 차이가 크다. API 응답 형식, DB 스키마 방향, 필드명, ID 전략이 아직 고정되지 않았다.
- 특히 DB 접속 정보 하드코딩, `create_all` 기반 자동 테이블 생성, placeholder route 위주 구조는 팀 협업 시 동일 결과 재현성을 해칠 가능성이 크다.

### 의존성 순서

- 첫 번째는 계약 고정이다. `agribot_interfaces`의 msg/srv/action, `API_명세서.md`, `DB_SCHEMA_초안.sql`의 이름과 필드를 먼저 맞춰야 한다.
- 두 번째는 ROS 코어 안정화다. TF, frame, LiDAR topic, waypoint 좌표계가 정리되어야 이후 perception/control/iot를 붙여도 덜 흔들린다.
- 세 번째는 가장 작은 E2E 슬라이스 구현이다. 관측 1개, 규칙 1개, 장치 1개를 잇는 최소 흐름부터 완성하는 편이 맞다.
- 네 번째는 MQTT, backend, WebSocket 실시간 계약 구현이다. 지금 단계에서는 frontend보다 서비스 계약과 런타임 연결이 먼저다.

### 프로젝트 마무리를 위한 단계별 구현 계획

- 1. 문서와 코드의 명칭 충돌을 먼저 정리한다. `missions/misson`, 필드명 불일치, integer id와 UUID 방향 차이를 먼저 고정한다.
- 2. ROS P0 안정화 패치를 한다. `map -> odom` 단일 소유자 결정, LiDAR topic 통일, 센서 TF 중복 제거, waypoint와 crop 좌표계 정렬이 우선이다.
- 3. `ExecuteActuation.srv`, `IoTDeviceState.msg` 같은 최소 인터페이스를 고정하고 `watering_controller_node` 하나로 command/result/state 폐루프를 먼저 만든다.
- 4. 그 위에 `mission_manager`를 올려 단일 규칙 기반 자동 제어를 붙인다.
- 5. 이후 `PlantObservation.msg`와 `detector_node`를 붙여 perception 이벤트가 실제 mission을 트리거하게 만든다.
- 6. 마지막에 MQTT bridge, backend 저장, WebSocket broadcast를 연결해 future client가 의존할 계약을 실제 동작으로 바꾼다.

### 참고 파일

- `/home/ssafy/SSAFY/S14P21A602/agribot_ws/src/agribot_bringup/launch/simulation.launch.py`
- `/home/ssafy/SSAFY/S14P21A602/agribot_ws/src/agribot_navigation/launch/navigation.launch.py`
- `/home/ssafy/SSAFY/S14P21A602/agribot_ws/src/agribot_navigation/launch/autonomous_mapping.launch.py`
- `/home/ssafy/SSAFY/S14P21A602/backend/main.py`
- `/home/ssafy/SSAFY/API_명세서.md`
- `/home/ssafy/SSAFY/DB_SCHEMA_초안.sql`
- `/home/ssafy/SSAFY/S14P21A602/docs/수확해조_프로젝트_개발_계획_및_협업_가이드.md`

### 비고

- 이번 문서 추가는 `2026-03-20 13:43:08 KST` 기준 진단 요약이다.
- 이번에는 코드 수정 없이 현 상태 진단과 우선순위 재정리에만 집중했다.

# AgriBot 프로젝트 진행 상황

## 2026-03-19

### 이번에 진행한 작업

- `agribot`의 목표 주행 속도를 `0.30 m/s`에서 `1.60 m/s`로 상향했다.
- 단순히 최고 속도만 올리지 않고, 그 변화에 따라 깨질 수 있는 Nav2 / Gazebo / SLAM / AMCL 관련 하드코딩된 파라미터들도 함께 조정했다.
- 변경 후 실제 런타임 검증까지 수행했고, 결과를 확인한 뒤 커밋했다.

### 수정한 파일

- `/home/ssafy/SSAFY/S14P21A602/agribot_ws/src/agribot_description/models/agribot/model.sdf`
- `/home/ssafy/SSAFY/S14P21A602/agribot_ws/src/agribot_navigation/config/nav2_params.yaml`
- `/home/ssafy/SSAFY/S14P21A602/agribot_ws/src/agribot_navigation/config/nav2_mapping_params.yaml`
- `/home/ssafy/SSAFY/S14P21A602/agribot_ws/src/agribot_navigation/config/slam_mapping.yaml`
- `/home/ssafy/SSAFY/S14P21A602/agribot_ws/src/agribot_navigation/config/amcl.yaml`
- `/home/ssafy/SSAFY/S14P21A602/agribot_ws/src/agribot_navigation/config/patrol_waypoints.yaml`

### 핵심 변경 내용

- Gazebo diff-drive 제한값
  - `max_linear_velocity: 0.30 -> 1.60`
  - `max_linear_acceleration: 0.60 -> 2.40`
  - `max_angular_velocity: 1.20 -> 3.20`
  - `max_angular_acceleration: 1.20 -> 3.20`
- LiDAR update rate
  - `10 Hz -> 20 Hz`
- Nav2 controller
  - `desired_linear_vel: 1.60`
  - `controller_frequency: 30 Hz`
  - lookahead / curvature / regulated scaling 관련 값들을 고속 주행 기준으로 재조정
- Costmap
  - local costmap 크기 `4x4 -> 8x8`
  - local/global update 주기 상향
  - obstacle range / inflation radius 확장
- SLAM / AMCL
  - 고속 이동 중 샘플 누락이 줄어들도록 최소 이동 거리, 최소 시간 간격, beam 수, update threshold 등을 조정
- 순찰 메타데이터
  - `recommended_travel_speed_mps: 1.60`으로 반영

### 검증 내용

- `pytest src/agribot_navigation/test/test_harvest_routing.py`
  - `3 passed`
- YAML 파싱 검증 통과
- `xmllint`로 `model.sdf` XML 검증 통과
- `compileall` 검증 통과
- 실제 런타임 검증도 진행
  - `autonomous_mapping.launch.py use_rviz:=false patrol_autostart:=false`로 스택 기동
  - 런타임에서 아래 값 확인
    - `controller_frequency = 30.0`
    - `FollowPath.desired_linear_vel = 1.6`
    - `FollowPath.lookahead_dist = 1.2`
    - `FollowPath.max_lookahead_dist = 2.6`
    - `FollowPath.regulated_linear_scaling_min_radius = 3.0`
  - `mapping_patrol/start` 호출 후 실제 관측값
    - 최대 선속도 `1.5997 m/s`
    - 최대 각속도 `1.9618 rad/s`

### 참고 사항

- 이번 수정은 "속도를 높였을 때 바로 무너지는 비율"을 줄이기 위한 튜닝이다.
- 다만 `1.6 m/s`는 기존 대비 상당히 공격적인 값이라서, 실제 데모/발표 전에는 직선 구간, 회전 구간, 온실 통로 충돌 여유를 다시 한 번 눈으로 확인하는 것이 좋다.
- LiDAR는 설정상 `20 Hz`로 올렸지만 이번 검증 런에서는 약 `15.7 Hz` 수준으로 측정됐다. 현재 Gazebo 실시간율과 렌더링 영향 가능성이 있다.

### 커밋

- 커밋 해시: `5b4a882`
- 메시지: `Tune agribot for 1.6 m/s navigation`

### 다시 빌드 / 실행할 때 명령어

가제보 실행 전 잔여 프로세스 정리:

```bash
cd /home/ssafy/SSAFY/S14P21A602/agribot_ws

# 좀비(Z) 자체는 직접 kill 되지 않으므로, Gazebo/ROS 잔여 프로세스를 먼저 정리
pkill -TERM -f 'gz sim|gzserver|gzclient|gazebo|ign gazebo|rviz2|slam_toolbox|planner_server|controller_server|bt_navigator|lifecycle_manager|map_server|amcl|component_container'
sleep 2
pkill -KILL -f 'gz sim|gzserver|gzclient|gazebo|ign gazebo|rviz2|slam_toolbox|planner_server|controller_server|bt_navigator|lifecycle_manager|map_server|amcl|component_container'

# Z 상태가 남아 있으면 관련 부모 프로세스까지 정리
ps -eo ppid=,stat=,cmd= \
  | awk '$2 ~ /^Z/ && $0 ~ /(agribot|gazebo|gz sim|gzserver|gzclient|rviz2|ros2 launch)/ {print $1}' \
  | sort -u \
  | xargs -r kill -KILL
```

빌드를 한 적이 없는 사람 기준:

```bash
cd /home/ssafy/SSAFY/S14P21A602/agribot_ws
source /opt/ros/jazzy/setup.bash
colcon build --packages-up-to agribot_bringup --symlink-install
source install/setup.bash
```

클린 빌드 기준:

```bash
cd /home/ssafy/SSAFY/S14P21A602/agribot_ws
rm -rf build install log
source /opt/ros/jazzy/setup.bash
colcon build --packages-up-to agribot_bringup --symlink-install
source install/setup.bash
```

- `ros2 launch agribot_bringup simulation.launch.py`를 쓸 때는 `agribot_bringup`까지의 의존 패키지를 함께 빌드해야 한다. `--packages-up-to agribot_bringup`이면 `agribot_interfaces`를 포함한 필요한 패키지가 같이 빌드된다.
- 처음 빌드와 클린 빌드의 차이는 `rm -rf build install log` 유무뿐이다.
- 이전 실패 빌드에서 현재 셸이 이미 꼬여 있다면 새 터미널에서 다시 시작하는 편이 가장 안전하다.

정적 맵 네비게이션:

```bash
ros2 launch agribot_bringup simulation.launch.py
```

SLAM 기반 자율 매핑:

```bash
ros2 launch agribot_navigation autonomous_mapping.launch.py
```

### 추가 보강: 시작 위치 bootstrap / stall 탈출

- 시작 직후 frontier 후보가 충분히 보이지 않거나 goal 선정이 실패하면, explorer가 바로 멈추지 않고 `초기 360도 spin`으로 주변을 먼저 스캔하도록 보강했다.
- 첫 spin 뒤에도 시작 위치에서 여전히 탐색이 안 열리면, `후진 -> 다시 360도 spin` 순서로 한 번 더 시작 탈출을 시도하게 했다.
- 좁은 곳에서 goal이 stalled / occupied 계열로 실패했을 때도, 다음 goal만 바로 고르는 대신 `제자리 회전 -> 후진` 탈출 시퀀스를 먼저 수행한 뒤 재탐색하게 했다.
- startup 단계에서는 cluster size와 minimum goal distance 기준을 완화해, 시작 위치가 넓든 좁든 초기 주행이 더 쉽게 열리도록 했다.

### 추가 진행한 작업

- 자율 매핑 기본 동작을 greenhouse 전용 waypoint patrol에서 일반 frontier exploration으로 바꿨다.
- 원인을 LiDAR 범위 부족 하나로 단정하지 않고, 탐색 방식, Nav2 controller, costmap, SLAM, AMCL, 센서 유효 범위를 함께 조정했다.
- 그래서 이제 기본 `autonomous_mapping.launch.py`는 다른 맵으로 바꿔도 frontier 기반으로 자율 탐색/매핑을 시도할 수 있다.

### 이번에 추가로 수정한 파일

- `/home/ssafy/SSAFY/S14P21A602/agribot_ws/src/agribot_navigation/agribot_navigation/frontier_explorer.py`
- `/home/ssafy/SSAFY/S14P21A602/agribot_ws/src/agribot_navigation/config/frontier_explorer.yaml`
- `/home/ssafy/SSAFY/S14P21A602/agribot_ws/src/agribot_navigation/test/test_frontier_explorer.py`
- `/home/ssafy/SSAFY/S14P21A602/agribot_ws/src/agribot_navigation/launch/autonomous_mapping.launch.py`
- `/home/ssafy/SSAFY/S14P21A602/agribot_ws/src/agribot_navigation/config/nav2_mapping_params.yaml`
- `/home/ssafy/SSAFY/S14P21A602/agribot_ws/src/agribot_navigation/config/slam_mapping.yaml`
- `/home/ssafy/SSAFY/S14P21A602/agribot_ws/src/agribot_navigation/config/amcl.yaml`
- `/home/ssafy/SSAFY/S14P21A602/agribot_ws/src/agribot_description/models/agribot/model.sdf`

### 추가 핵심 변경 내용

- 기본 자율 매핑 launch
  - `use_frontier_explorer:=true`
  - `use_patrol:=false`
  - `use_boundary_map:=true`
- 새 frontier explorer
  - live `/map` 기준 frontier cluster 탐색
  - 실패 goal blacklist 후 재시도
  - 상태 topic: `/mapping_explorer/status`
  - 서비스: `/mapping_explorer/start`, `/mapping_explorer/stop`, `/mapping_explorer/resume`
- 센서/맵 관련 값
  - LiDAR samples `360 -> 720`
  - LiDAR max range `10.0 -> 25.0`
  - RGB-D far clip `10.0 -> 25.0`
  - SLAM / AMCL laser range도 `25.0` 기준으로 조정
- mapping 모드 Nav2
  - `use_rotate_to_heading: false`
  - lookahead / tolerance / local/global costmap 범위 재조정
  - 막힌 frontier에서 즉시 끝나지 않고 다른 frontier로 계속 진행

### 추가 검증 내용

- `python3 -m pytest src/agribot_navigation/test/test_harvest_routing.py src/agribot_navigation/test/test_frontier_explorer.py`
  - `6 passed`
- `colcon build --packages-select agribot_navigation agribot_description agribot_bringup --symlink-install`
  - 통과
- 실제 런타임 검증
  - `ros2 launch agribot_navigation autonomous_mapping.launch.py use_rviz:=false`
  - frontier goal 생성 확인
  - `/mapping_explorer/status`가 `exploring`으로 유지되는 것 확인
  - 최대 선속도 약 `1.5155 m/s`
  - planner에서 `Start occupied`가 떠도 blacklist 후 다음 frontier로 재시도하는 것 확인

### 추가 커밋

- 커밋 해시: `7185809`
- 메시지: `Add frontier-based autonomous mapping explorer`

### 지금 기준으로 다시 빌드 / 실행할 때 명령어

빌드를 한 적이 없는 사람 기준:

```bash
cd /home/ssafy/SSAFY/S14P21A602/agribot_ws
source /opt/ros/jazzy/setup.bash
colcon build --packages-up-to agribot_navigation --symlink-install
source install/setup.bash
export DRI_PRIME=1
export __NV_PRIME_RENDER_OFFLOAD=1
export __GLX_VENDOR_LIBRARY_NAME=nvidia
```

클린 빌드 기준:

```bash
cd /home/ssafy/SSAFY/S14P21A602/agribot_ws
rm -rf build install log
source /opt/ros/jazzy/setup.bash
colcon build --packages-up-to agribot_navigation --symlink-install
source install/setup.bash
export DRI_PRIME=1
export __NV_PRIME_RENDER_OFFLOAD=1
export __GLX_VENDOR_LIBRARY_NAME=nvidia
```

- `ros2 launch agribot_navigation autonomous_mapping.launch.py`만 실행할 때는 `agribot_navigation`까지의 의존 패키지만 빌드하면 충분하다. 이 명령은 `agribot_interfaces`, `agribot_description`까지 자동으로 포함한다.
- 처음 빌드와 클린 빌드의 차이는 `rm -rf build install log` 유무뿐이다.
- 이전 실패 빌드에서 `source install/setup.bash`를 이미 실행한 셸이면 없는 install 경로 경고가 남을 수 있다. 이 경우 새 터미널에서 다시 시작하면 가장 깔끔하다.

직접 자율주행으로 맵을 그려보고 싶다면:

```bash
ros2 launch agribot_navigation autonomous_mapping.launch.py
```

설명:

- 현재 코드 기본값은 `use_frontier_explorer:=true`, `frontier_autostart:=true`, `use_patrol:=false`, `use_boundary_map:=true`라서 추가 인자 없이 frontier 기반 자율 매핑이 바로 시작된다.
- `use_rviz:=true`가 기본값이라 RViz에서 실시간으로 맵이 그려지는 것을 바로 볼 수 있다.
- 현재는 `use_boundary_map:=true`가 기본값이라 비닐하우스 입구를 포함한 외곽 경계를 기본적으로 가상 벽처럼 취급한다.
- 온실 바깥까지 일부러 탐색하고 싶을 때만 `use_boundary_map:=false`로 끄면 된다.

GUI 없이 빠르게 돌려보고 싶다면:

```bash
ros2 launch agribot_navigation autonomous_mapping.launch.py use_rviz:=false
```

온실 경계 가상 벽을 끄고 바깥까지 탐색하게 하고 싶다면:

```bash
ros2 launch agribot_navigation autonomous_mapping.launch.py \
  use_boundary_map:=false \
  use_rviz:=false
```

frontier explorer 상태 확인 / 수동 제어:

```bash
ros2 topic echo /mapping_explorer/status
ros2 service call /mapping_explorer/start std_srvs/srv/Trigger "{}"
ros2 service call /mapping_explorer/stop std_srvs/srv/Trigger "{}"
ros2 service call /mapping_explorer/resume std_srvs/srv/Trigger "{}"
```

맵이 어느 정도 채워졌다면, 다른 터미널에서 저장:

```bash
cd /home/ssafy/SSAFY/S14P21A602/agribot_ws
source /opt/ros/jazzy/setup.bash
source install/setup.bash

ros2 run nav2_map_server map_saver_cli -f \
  /home/ssafy/SSAFY/S14P21A602/agribot_ws/src/agribot_navigation/maps/greenhouse_map_20260320_test
```

저장 결과:

- `greenhouse_map_20260320_test.yaml`
- `greenhouse_map_20260320_test.pgm`

먼저는 이렇게 새 파일명으로 저장해서 확인하는 편이 안전하다.
- 기존 `greenhouse_map.yaml`을 바로 덮어쓰면 팀원이 pull 받은 기준 맵과 차이가 커질 수 있다.
- 저장이 잘 됐는지 보려면 아래처럼 미리보기 런치를 쓰면 된다.

```bash
ros2 launch agribot_navigation map_preview.launch.py \
  map:=/home/ssafy/SSAFY/S14P21A602/agribot_ws/src/agribot_navigation/maps/greenhouse_map_20260320_test.yaml
```

기존 greenhouse patrol 방식으로 다시 실행하고 싶다면:

```bash
ros2 launch agribot_navigation autonomous_mapping.launch.py \
  use_patrol:=true \
  patrol_autostart:=true \
  use_frontier_explorer:=false \
  use_boundary_map:=true \
  use_rviz:=false
```

### 왜 RViz 맵이 로보락처럼 깔끔하지 않고 지저분해 보이는가

지금 보이는 화면은 "사용자용으로 다듬어진 최종 맵"이 아니라, 개발 중 디버깅을 위해 raw 정보가 많이 겹쳐진 RViz 화면에 가깝다.

- RViz 설정상 `/map`만 보이는 것이 아니라 `LaserScan`, `Global Costmap`, `Local Costmap`도 함께 켜져 있다.
  - 그래서 흰 점 형태의 raw scan, costmap의 보라/하늘색 영역, SLAM occupancy map이 한 화면에 겹쳐 보여 더 복잡하게 보인다.
- 현재 온실 월드는 벽만 라이다에 잡히는 구조가 아니다.
  - `tomato_plant/model.sdf`에 식물 줄기/덩굴 덩어리를 따라가는 collision box를 넣어, 2D LiDAR가 작물도 지속적인 장애물로 인식하도록 해두었다.
  - 즉 작물 자체가 맵에 남는 것이 의도된 구조다.
- `farm_world.sdf`에도 온실 외벽과 재배 베드를 단순 collision proxy로 따로 넣어두었다.
  - 그래서 실내 청소기처럼 벽 위주의 단순 평면도가 아니라, 통로 + 베드 + 작물 구조가 함께 반영된다.
- LiDAR 설정도 비교적 공격적이다.
  - 현재 GPU LiDAR는 `720 samples`, `25.0m range`로 설정되어 있어 먼 구조물과 주변 장애물을 많이 주워온다.
  - 이는 "예쁜 지도"보다 "실제 주행 판단에 필요한 장애물 보존" 쪽에 더 가깝다.
- SLAM Toolbox 쪽에도 로보락 같은 consumer용 map beautification 단계는 없다.
  - 현재는 `0.05m` 해상도의 raw occupancy map을 실시간으로 쌓는 구조라, 벽 직선화/노이즈 제거/방 분할 같은 후처리가 자동으로 들어가지 않는다.
- 추가로 문서화된 현재 제한 사항대로 `farm_world.sdf`의 Gazebo 물리 부하 때문에 `odom -> base_link` TF가 늦거나 흔들릴 수 있다.
  - 이런 오도메트리/TF 흔들림은 scan 누적 결과를 퍼지게 만들어 맵 가장자리를 더 지저분하게 보이게 할 수 있다.

정리하면, 지금 맵이 지저분해 보이는 것은 LiDAR가 이상해서라기보다
"raw scan + live SLAM map + Nav2 costmap + 작물 장애물 반영 + TF 흔들림 가능성"이 함께 보이는 개발용 화면이기 때문이다.

반대로 로보락 앱에 보이는 맵은 보통 아래 후처리를 거친 사용자용 결과물에 가깝다.

- 벽 직선화
- 작은 노이즈 제거
- 일시 장애물 억제
- 방 단위 분할
- UI용 단순화 렌더링

즉 현재 AgriBot 맵은 "깔끔한 집 평면도"를 우선한 것이 아니라, 온실 자율주행에 필요한 실제 장애물 정보를 더 많이 살리는 방향으로 만들어져 있다고 보는 편이 맞다.

## 2026-03-20 추가 작업

### 협소 구간 자율 매핑 안정화

- 막다른 통로, 식물 베드 사이의 좁은 통로, `Start occupied`가 자주 뜨는 포켓 구간에서 frontier explorer가 같은 함정을 반복해서 찌르거나 너무 공격적인 goal을 주는 문제가 있었다.
- 이번에는 단순히 backout 거리만 늘리는 방식이 아니라, "frontier 끝점"과 "실제로 goal을 줄 staging 지점"을 분리하는 쪽으로 알고리즘을 바꿨다.
- 목표는 로봇청소기처럼 한 번 막힌 포켓을 바로 재시도하지 말고, 조금 더 넓고 안전한 지점에서 다시 맵을 확장하도록 만드는 것이다.

### 이번에 수정한 핵심 내용

- `frontier_explorer.py`
  - frontier cluster마다 frontier 끝점 그대로 goal을 주지 않고, 주변 free cell을 탐색해서 더 넓고 여유 있는 staging cell을 고르도록 변경했다.
  - goal 후보 점수에 `clearance`, `support area`, `frontier와의 오프셋`을 함께 반영해 좁은 막다른 tip보다 넓은 접근 지점을 우선하게 했다.
  - 실패한 goal은 점 하나가 아니라 일정 반경의 trap region으로 잠시 blacklist하고, 일정 시간 뒤 다시 재도전 가능하게 바꿨다.
  - 현재 blacklist 때문에 모든 후보가 막혀 있으면 exploration을 종료하지 않고 cooldown 상태로 기다리도록 변경했다.
- `nav2_mapping_params.yaml`
  - mapping 모드 선속도와 lookahead를 낮추고, 장애물 근처 감속을 활성화해 협소 구간에서 덜 공격적으로 움직이게 했다.
  - global costmap 해상도를 더 촘촘하게 두고, footprint padding / inflation을 좁은 통로 기준으로 다시 맞췄다.
  - progress checker 허용치를 늘려, 좁은 공간에서 천천히 비비며 빠져나오는 상황을 너무 빨리 stalled로 오판하지 않게 했다.
- `navigate_to_pose_w_backout_recovery.xml`
  - recovery retry 수를 늘리고, 더 긴 backout 단계를 하나 더 추가했다.
- `navigate_through_poses_w_backout_recovery.xml`
  - 위와 같은 추가 long backout recovery를 waypoint 계열 BT에도 같이 반영했다.

### 검증

- `python3 -m pytest /home/ssafy/SSAFY/S14P21A602/agribot_ws/src/agribot_navigation/test/test_frontier_explorer.py`
  - `5 passed`
- `python3 -m py_compile /home/ssafy/SSAFY/S14P21A602/agribot_ws/src/agribot_navigation/agribot_navigation/frontier_explorer.py`
  - 통과
- `colcon build --packages-up-to agribot_navigation --symlink-install`
  - 통과

## 2026-03-20 추가 작업 2

### 맵 교체에도 버티는 일반화된 자율 매핑 보강

- 이번에는 특정 온실 맵 한 장을 기준으로 goal 점수만 만지는 방식이 아니라, 다른 맵으로 바꿔도 그대로 쓰일 수 있는 탐색/복구 구조 쪽을 보강했다.
- 실제 런에서 확인된 핵심 문제는 두 가지였다.
  - frontier explorer가 `map -> base_link`가 열리기 전까지는 startup bootstrap도 못 하고 `Waiting for transform`에 머무를 수 있었다.
  - `/odom` 타임스탬프가 불안정할 때 `odom -> base_link` TF가 뒤로 가는 시간이 섞여 TF 트리 전체가 흔들릴 수 있었다.
- 이 둘이 겹치면 시작 위치가 넓든 좁든 상관없이 "첫 움직임 자체가 안 열리는" 상황이 생길 수 있다.

### 이번에 추가로 수정한 핵심 내용

- `frontier_explorer.py`
  - startup 단계에서는 `map`이나 `map -> base_link` transform이 아직 없더라도 `초기 360도 spin`, `후진 후 재스캔` bootstrap recovery를 먼저 실행할 수 있게 바꿨다.
  - 즉 "맵이 있어야 움직인다"가 아니라, "맵이 부족하면 먼저 움직여서 맵을 연다" 쪽으로 순서를 뒤집었다.
  - frontier 후보가 한동안 아예 보이지 않을 때는 곧바로 exploration 완료로 끝내지 않고, `재스캔 spin`, `후진 + 재스캔`을 순차적으로 수행해 숨은 frontier를 다시 열어보게 했다.
  - transform lookup이 잠깐 흔들릴 때는 최근 pose를 짧게 재사용하도록 해, 일시적인 TF 공백 때문에 탐색 루프가 바로 멈추지 않게 했다.
  - 상태 topic에 `startup_attempts`, `frontier_refresh_attempts`를 추가해 실제 런에서 어느 recovery 단계가 동작했는지 바로 볼 수 있게 했다.
- `odom_tf_broadcaster.py`
  - `/odom` 메시지의 stamp가 비어 있거나 이전보다 과거로 가는 경우, 그 transform을 TF에 내보내지 않도록 바꿨다.
  - 목적은 TF 트리의 시간축을 단조롭게 유지해서 SLAM / Nav2 / explorer가 같은 시계를 보게 만드는 것이다.
- `spawn_agribot.launch.py`
  - `odom_tf_broadcaster`, `cmd_vel_watchdog`, 정적 TF publisher들에도 `use_sim_time:=true`를 명시해 시뮬레이션 시간 기준을 통일했다.

### 왜 이 방식이 더 일반적인가

- 특정 맵의 frontier 좌표를 하드코딩하지 않는다.
- 특정 온실 구조를 전제로 한 patrol waypoint에 의존하지 않는다.
- 시작 위치가 탁 트였는지, 벽 옆인지, 막다른 포켓인지와 무관하게 "맵이 안 열리면 먼저 자세와 위치를 바꿔 다시 본다"는 공통 전략을 쓴다.
- frontier가 일시적으로 사라져도 바로 종료하지 않고, 한 번 더 스캔하고 한 번 더 빠져나오며 지도 확장 가능성을 확인한다.
- TF가 잠깐 흔들려도 즉시 정지하지 않고, 복구 가능한 수준의 일시 오류는 흡수한다.

### 실제 런 검증 결과

- `ros2 launch agribot_navigation autonomous_mapping.launch.py use_rviz:=false`로 다시 검증했다.
- 실제 로그에서 아래 순서가 확인됐다.
  - `초기 360도 스캔으로 주변 frontier를 확보합니다.`
  - `spin completed successfully`
  - `Navigating to frontier goal ...`
  - `Exploring toward frontier goal ...`
  - `Reached the goal!`
  - `Frontier goal reached; selecting the next frontier.`
- 상태 topic도 아래처럼 바뀌는 것을 확인했다.
  - `state: exploring`
  - `startup_attempts: 1`
  - `frontier_refresh_attempts: 0`
  - `active_goal: [...]`

### 검증

- `python3 -m pytest src/agribot_navigation/test/test_frontier_explorer.py src/agribot_description/test/test_odom_tf_broadcaster.py`
  - `9 passed`
- `python3 -m py_compile /home/ssafy/SSAFY/S14P21A602/agribot_ws/src/agribot_navigation/agribot_navigation/frontier_explorer.py /home/ssafy/SSAFY/S14P21A602/agribot_ws/src/agribot_description/agribot_description/odom_tf_broadcaster.py`
  - 통과
- `colcon build --packages-up-to agribot_navigation --symlink-install`
  - 통과

### 실행 명령어 변경 여부

- 이번 2026-03-20 추가 작업들로도 실행 명령 자체는 바뀌지 않았다.
- 자율 매핑 실행과 `use_rviz:=false` 빠른 확인 명령은 위 `직접 자율주행으로 맵을 그려보고 싶다면` 절을 그대로 따르면 된다.
