# 수확해조 포팅 매뉴얼

작성일: 2026-03-30

## 1. 프로젝트 개요

- 프로젝트명: 수확해조(AgriBot)
- 저장소 루트: `/home/ssafy/Desktop/pjt/S14P21A602`
- 주요 구성:
  - `agribot_ws`: ROS 2 Jazzy 기반 시뮬레이션/내비게이션/제어
  - `backend`: FastAPI + SQLAlchemy + PostgreSQL 기반 백엔드
  - `frontend`: React + Vite 기반 웹 대시보드
  - `artifacts`: 모델 파일, 런타임 산출물, 시연 입력 이미지

## 2. 사용 제품 및 버전

### 2.1 시스템 / 개발 도구

| 항목 | 제품 | 버전 | 근거 |
| --- | --- | --- | --- |
| OS | Ubuntu | 24.04 | `README.md` |
| IDE | Visual Studio Code | 1.111.0 | `.vscode/settings.json`, `code --version` |
| JVM | 프로젝트 기준 미사용 | N/A | Java/Gradle/Maven 산출물 없음 |
| 참고 JVM | OpenJDK | 21.0.10 | `java -version` |
| Node.js | Node.js | 24.14.0 | `node -v` |
| npm | npm | 11.9.0 | `npm -v` |
| Docker Engine | Docker | 29.3.0 | `docker --version` |
| Docker Compose | Docker Compose | v5.1.0 | `docker compose version` |

### 2.2 애플리케이션 / 런타임

| 항목 | 제품 | 버전 | 비고 |
| --- | --- | --- | --- |
| ROS 배포판 | ROS 2 Jazzy | Jazzy | `README.md`, `scripts/agribot_env.sh` |
| 시뮬레이터 | Gazebo Harmonic | Gazebo Sim 8.10.0 | `README.md`, `gz sim --version` |
| 언어 런타임 | Python | 3.12.3 | `README.md`, `python3 --version` |
| 프론트엔드 | React | 18.3.1 | `frontend/package.json` |
| 프론트엔드 빌드 도구 | Vite | 5.4.10 | `frontend/package.json` |
| 타입체커 | TypeScript | 5.6.2 | `frontend/package.json` |
| 백엔드 프레임워크 | FastAPI | 0.135.2 | `backend/requirements.txt` |
| WAS/ASGI 서버 | Uvicorn | 0.42.0 | `backend/requirements.txt`, `scripts/backend_up.sh` |
| ORM | SQLAlchemy | 2.0.48 | `backend/requirements.txt` |
| PostgreSQL 드라이버 | psycopg2-binary | 2.9.11 | `backend/requirements.txt` |
| 추론 라이브러리 | Ultralytics | 8.4.27 | `backend/requirements.txt` |
| DBMS | PostgreSQL | 16.13 | `backend/docker-compose.yml`, `SELECT version();` |
| DB 관리도구 | pgAdmin4 | 미고정(floating tag) | `backend/docker-compose.yml`에서 `dpage/pgadmin4` 사용 |
| MQTT 브로커 | Eclipse Mosquitto | 2.1.2 | `backend/docker-compose.yml`, `mosquitto -h` |
| 웹서버 | 별도 전용 웹서버 미구성 | N/A | Nginx/Apache 설정 없음 |
| 개발용 웹서버 | Vite Dev Server | 5.4.10 | `npm run dev`, `scripts/frontend_up.sh` |

## 3. 포팅 및 실행 절차

### 3.1 사전 요구사항

- ROS 2 Jazzy 설치
- Gazebo Harmonic 및 `ros-gz` 설치
- Navigation2, SLAM Toolbox 설치
- Docker / Docker Compose 설치
- Node.js / npm 설치
- Python 3.12 및 `venv` 사용 가능 환경

### 3.2 최초 1회 준비

#### Frontend

```bash
cd /home/ssafy/Desktop/pjt/S14P21A602/frontend
npm install
npm run build
```

#### Backend

```bash
cd /home/ssafy/Desktop/pjt/S14P21A602/backend
python3 -m venv .venv
source .venv/bin/activate
python -m pip install --upgrade pip
python -m pip install -r requirements.txt
../scripts/install_backend_runtime.sh
```

#### ROS Workspace

```bash
cd /home/ssafy/Desktop/pjt/S14P21A602/agribot_ws
source /opt/ros/jazzy/setup.bash
colcon build --symlink-install
source install/setup.bash
```

### 3.3 기본 실행 순서

```bash
cd /home/ssafy/Desktop/pjt/S14P21A602
source <(./scripts/operator_runtime_health.sh --print-env)
./scripts/operator_runtime_health.sh --reset-runtime
./scripts/backend_up.sh
./scripts/frontend_up.sh
./scripts/agribot_launch.sh agribot_bringup simulation.launch.py use_iot:=true
./scripts/mission_manager_up.sh
./scripts/harvest_action_server_up.sh
```

## 4. 빌드 및 실행 시 사용되는 환경변수

### 4.1 공통 / ROS / 런타임

| 변수명 | 기본값 | 사용 위치 | 설명 |
| --- | --- | --- | --- |
| `REPO_ROOT` | 저장소 루트 자동 계산 | `scripts/agribot_env.sh` | 저장소 최상위 경로 |
| `AGRIBOT_WS` | `${REPO_ROOT}/agribot_ws` | `scripts/agribot_env.sh` | Colcon 워크스페이스 경로 |
| `ROS_DISTRO` | `jazzy` | `scripts/agribot_env.sh` | ROS 배포판 이름 |
| `AGRIBOT_RUNTIME_DIR` | `/tmp/agribot_runtime` | `scripts/agribot_env.sh`, `backend/robot_runtime_state_service.py` | Backend와 ROS 노드가 공유하는 런타임 JSON 파일 저장 경로 |
| `AGRIBOT_BACKEND_RUNTIME_DIR` | `${REPO_ROOT}/artifacts/runtime/backend` | `scripts/agribot_env.sh`, 추론 서비스 | 추론 결과 이미지/메타데이터 저장 경로 |
| `AGRIBOT_PERCEPTION_PYTHON` | `${REPO_ROOT}/backend/.venv/bin/python` | `scripts/agribot_env.sh` | 추론 런타임에서 사용할 Python 인터프리터 |
| `AGRIBOT_GRAPHICS_PROFILE` | `system` | `scripts/agribot_env.sh`, `agribot_bringup/launch_profile.py` | Gazebo/렌더링용 그래픽 프로파일 |
| `AGRIBOT_PERFORMANCE_MODE` | `balanced` | `scripts/agribot_env.sh`, `agribot_bringup/launch_profile.py` | 시뮬레이터 성능 튜닝 프로파일 |
| `AGRIBOT_ROS_DISCOVERY_RANGE` | `LOCALHOST` | `scripts/agribot_env.sh`, `agribot_bringup/launch_profile.py` | ROS discovery 범위 기본값 |
| `ROS_AUTOMATIC_DISCOVERY_RANGE` | `${AGRIBOT_ROS_DISCOVERY_RANGE}` | `scripts/agribot_env.sh` | Jazzy 권장 discovery 환경변수 |
| `BACKEND_HOST` | `127.0.0.1` | `scripts/agribot_env.sh`, `scripts/backend_up.sh` | Uvicorn 바인딩 주소 |
| `BACKEND_PORT` | `8000` | `scripts/agribot_env.sh`, `scripts/backend_up.sh` | Backend 포트 |
| `FRONTEND_HOST` | `127.0.0.1` | `scripts/agribot_env.sh`, `scripts/frontend_up.sh` | Vite 바인딩 주소 |
| `FRONTEND_PORT` | `5173` | `scripts/agribot_env.sh`, `scripts/frontend_up.sh` | Frontend 포트 |
| `AGRIBOT_GZ_PARTITION` | `agribot_${USER:-local}_sim` | `scripts/agribot_env.sh` | Gazebo 통신 파티션 |
| `GZ_PARTITION` | `${AGRIBOT_GZ_PARTITION}` | `scripts/agribot_env.sh` | Gazebo 파티션 실제 적용값 |
| `AGRIBOT_GZ_IP` | `127.0.0.1` | `scripts/agribot_env.sh`, `agribot_bringup/launch_profile.py` | Gazebo 통신 기본 IP 원본 값 |
| `GZ_IP` | `127.0.0.1` | `scripts/agribot_env.sh` | Gazebo 통신 IP |
| `IGN_IP` | `127.0.0.1` | `scripts/agribot_env.sh` | Gazebo/IGN 호환용 IP |
| `ROS_DOMAIN_ID` | `42` | `scripts/agribot_env.sh` | 로컬 ROS graph 격리용 도메인 ID |
| `ROS_LOCALHOST_ONLY` | 사전 설정된 경우만 유지 | `scripts/agribot_env.sh` | 구형 ROS env 호환용. 기본적으로는 export하지 않음 |

### 4.2 Frontend 빌드 환경변수

| 변수명 | 기본값 | 정의 파일 | 설명 |
| --- | --- | --- | --- |
| `VITE_APP_NAME` | 예시값 `수확해조 Control Center`, 코드 fallback `수확해조 관제 센터` | `frontend/.env.example`, `frontend/src/config/env.ts` | 프론트 앱 표시 이름 |
| `VITE_API_BASE_URL` | `http://localhost:8000/api/v1` | `frontend/.env.example`, `frontend/src/config/env.ts` | Backend REST API 기본 URL |
| `VITE_WS_URL` | `ws://localhost:8000/ws/live` | `frontend/.env.example`, `frontend/src/config/env.ts` | WebSocket URL |

### 4.3 Backend / DB 환경변수

| 변수명 | 기본값 | 정의 파일 | 설명 |
| --- | --- | --- | --- |
| `AGRIBOT_DATABASE_URL` | `postgresql://agribot:password@localhost:5432/agribot_db` | `backend/database.py` | SQLAlchemy 접속 URL |
| `POSTGRES_USER` | `agribot` | `backend/docker-compose.yml` | PostgreSQL 계정 |
| `POSTGRES_PASSWORD` | `password` | `backend/docker-compose.yml` | PostgreSQL 비밀번호 |
| `POSTGRES_DB` | `agribot_db` | `backend/docker-compose.yml` | 기본 DB 이름 |
| `PGADMIN_DEFAULT_EMAIL` | `admin@admin.com` | `backend/docker-compose.yml` | pgAdmin 로그인 계정 |
| `PGADMIN_DEFAULT_PASSWORD` | `admin` | `backend/docker-compose.yml` | pgAdmin 로그인 비밀번호 |
| `BACKEND_URL` | `http://${BACKEND_HOST}:${BACKEND_PORT}` | `scripts/operator_runtime_health.sh` | 상태 점검 스크립트에서 사용할 Backend URL |
| `FRONTEND_URL` | `http://${FRONTEND_HOST}:${FRONTEND_PORT}` | `scripts/operator_runtime_health.sh` | 상태 점검 스크립트에서 사용할 Frontend URL |

### 4.4 추론 / 제어 / IoT 관련 주요 환경변수

| 변수명 | 기본값 | 정의 파일 | 설명 |
| --- | --- | --- | --- |
| `AGRIBOT_TOMATO_DISEASE_MODEL_PATH` | `${REPO_ROOT}/artifacts/models/tomato_disease/v1/best.pt` | `backend/services/perception/inference_service.py` | 병해 진단 모델 경로 |
| `AGRIBOT_TOMATO_MODEL_PATH` | 위 변수의 legacy alias | `backend/services/perception/inference_service.py` | 구버전 호환 모델 경로 |
| `AGRIBOT_TOMATO_DISEASE_DEVICE` | `auto` | `backend/services/perception/inference_service.py` | 추론 디바이스 지정 |
| `AGRIBOT_TOMATO_MODEL_DEVICE` | `auto` | `backend/services/perception/inference_service.py` | legacy alias |
| `AGRIBOT_MAIN_MODEL_NAME` | `tomato_disease_detector` | `backend/services/perception/inference_service.py` | 병해 추론 결과에 기록되는 모델명 |
| `AGRIBOT_MAIN_MODEL_VERSION` | `v1` | `backend/services/perception/inference_service.py` | 병해 추론 결과에 기록되는 모델 버전 |
| `AGRIBOT_MAIN_IMGSZ` | `960` | `backend/services/perception/inference_service.py` | YOLO 입력 이미지 크기 |
| `AGRIBOT_MAIN_CONFIDENCE` | `0.25` | `backend/services/perception/inference_service.py` | 추론 confidence threshold |
| `AGRIBOT_MAIN_IGNORED_CLASSES` | `healthy,normal,normal_leaf,healthy_leaf` | `backend/services/perception/inference_service.py` | 최종 판정에서 제외할 클래스 목록 |
| `AGRIBOT_WORLD_SDF_PATH` | `agribot_ws/src/agribot_description/worlds/farm_world.sdf` | `backend/services/actuation/rule_engine.py` | 치료 rule engine에서 참조하는 월드 파일 |
| `AGRIBOT_SPRINKLER_ZONE_ID` | `farm_01` | `backend/services/actuation/rule_engine.py` | 스프링클러 기본 zone ID |
| `AGRIBOT_ROS_SETUP_SCRIPT` | `/opt/ros/jazzy/setup.bash` | `backend/services/actuation/dispatcher.py` | ROS 환경 로드 스크립트 |
| `AGRIBOT_WS_SETUP_SCRIPT` | `${REPO_ROOT}/agribot_ws/install/setup.bash` | `backend/services/actuation/dispatcher.py` | 빌드된 워크스페이스 환경 스크립트 |
| `AGRIBOT_ENV_SETUP_SCRIPT` | `${REPO_ROOT}/scripts/agribot_env.sh` | `backend/services/actuation/dispatcher.py` | 공통 env 스크립트 |
| `AGRIBOT_IOT_COMMAND_PUBLISHER` | `${REPO_ROOT}/scripts/publish_iot_command.py` | `backend/services/actuation/dispatcher.py` | IoT 명령 publish 스크립트 경로 |
| `AGRIBOT_IOT_MANUAL_COMMAND_TOPIC` | `/iot/commands/manual` | `backend/services/actuation/dispatcher.py` | 수동 IoT 명령 토픽 |
| `AGRIBOT_IOT_AUTOMATIC_COMMAND_TOPIC` | `/iot/commands/auto` | `backend/services/actuation/dispatcher.py` | 자동 처방 IoT 명령 토픽 |
| `AGRIBOT_IOT_DISPATCH_COMMAND_TOPIC` | `/iot/commands/dispatch` | `backend/services/actuation/dispatcher.py` | 실제 dispatch 토픽 |
| `AGRIBOT_TREATMENT_DISPATCH_TIMEOUT_SEC` | `10.0` | `backend/services/actuation/dispatcher.py` | IoT 명령 publish timeout |
| `AGRIBOT_TREATMENT_DISPATCH_MIN_SUBSCRIBERS` | `1` | `backend/services/actuation/dispatcher.py`, `scripts/backend_up.sh` | 자동 치료 명령 전 최소 subscriber 수 |
| `AGRIBOT_MANUAL_COMMAND_MIN_SUBSCRIBERS` | `1` | `backend/services/actuation/dispatcher.py` | 수동 명령 publish 최소 subscriber 수 |
| `AGRIBOT_TREATMENT_REQUESTED_BY` | `backend:treatment_rule_engine` | `backend/services/actuation/dispatcher.py` | 요청자 식별 문자열 |

### 4.5 선택적 GPU/WSL2 관련 환경변수

아래 값은 저장소 문서에만 명시된 선택 항목이며, GPU 사용 또는 WSL2 가속 시에만 적용한다.

| 변수명 | 예시값 | 출처 | 설명 |
| --- | --- | --- | --- |
| `CUDA_VISIBLE_DEVICES` | `0` | `docs/프론트엔드 켜놓은 후 시연용 명령어 모음.md` | 추론 시 사용할 GPU 선택 |
| `GALLIUM_DRIVER` | `d3d12` | `README.md` | WSL2 GPU 가속 |
| `MESA_D3D12_DEFAULT_ADAPTER_NAME` | `NVIDIA` | `README.md` | WSL2 NVIDIA 어댑터 지정 |

## 5. 배포 시 특이사항

1. `scripts/agribot_env.sh`는 실행이 아니라 반드시 `source scripts/agribot_env.sh`로 불러와야 한다.
2. Backend와 ROS 노드는 반드시 같은 `AGRIBOT_RUNTIME_DIR`를 바라봐야 한다. 이 값이 다르면 프론트 버튼은 눌리지만 실제 상태 파일이 갱신되지 않는다.
3. Frontend는 Backend가 정적 파일을 서빙하지 않는다. 현재 저장소 기준 배포는 `npm run dev` 기반이며, 별도 운영 웹서버(Nginx/Apache) 설정은 포함되어 있지 않다.
4. Backend CORS 허용 origin이 `http://localhost:5173`, `http://127.0.0.1:5173`로 하드코딩되어 있다. 원격 배포 시 `backend/main.py` 수정이 필요하다.
5. `scripts/backend_up.sh`는 PostgreSQL, Mosquitto, pgAdmin 컨테이너를 먼저 띄우고, 기존 Uvicorn 프로세스 및 포트 점유 프로세스를 종료한 뒤 재기동한다.
6. `scripts/frontend_up.sh`도 기존 `5173` 포트를 점유한 프로세스를 종료한 뒤 Vite dev server를 올린다.
7. `scripts/agribot_launch.sh`는 이전 시뮬레이션 잔여 프로세스를 정리하고, launch 파일이 `runtime_dir` 인자를 지원하면 자동으로 `runtime_dir:=${AGRIBOT_RUNTIME_DIR}`를 주입한다.
8. 병해 모델은 기본적으로 `artifacts/models/tomato_disease/v1/best.pt`를 찾는다. 다른 경로를 쓰면 `AGRIBOT_TOMATO_DISEASE_MODEL_PATH` 또는 `AGRIBOT_TOMATO_MODEL_PATH`를 지정해야 한다.
9. 발표/시연 전에는 `./scripts/operator_runtime_health.sh --reset-runtime` 또는 `rm -rf "$AGRIBOT_RUNTIME_DIR"`로 런타임 파일을 초기화해야 이전 상태가 남지 않는다.
10. 현재 하드코딩 맵 기반 자율주행 모드에서는 LiDAR obstacle layer와 정적 맵 정렬 이슈 때문에 obstacle layer를 비활성화하는 운영 전제가 있다. 즉, 데모/포팅 시 live obstacle 회피까지 기대하면 안 된다.
11. DB 접속 정보는 개발용 기본값(`agribot/password`)으로 고정돼 있으므로 실제 운영 환경에서는 별도 시크릿 관리가 필요하다.
12. 원격 데스크톱/헤드리스 검증 환경에서는 `./scripts/agribot_launch.sh agribot_bringup simulation.launch.py use_iot:=true use_rviz:=false`를 권장한다. Gazebo GUI config/EGL 경고가 보여도 core ROS 노드와 file bridge는 정상 기동할 수 있다.

## 6. DB 접속 정보

### 6.1 기본 접속 정보

| 항목 | 값 | 비고 |
| --- | --- | --- |
| DBMS | PostgreSQL 16.13 | 컨테이너 실행 중 확인 |
| Host(호스트에서 접속) | `localhost` | `backend/database.py` 기본값 |
| Host(컨테이너 내부/pgAdmin) | `postgres` | `backend/pgadmin/servers.json` |
| Port | `5432` | `backend/docker-compose.yml` |
| Database | `agribot_db` | `backend/docker-compose.yml` |
| Username | `agribot` | `backend/docker-compose.yml` |
| Password | `password` | `backend/docker-compose.yml` |
| pgAdmin URL | `http://localhost:5050` | `backend/docker-compose.yml` |
| pgAdmin 계정 | `admin@admin.com / admin` | 개발용 계정 |

### 6.2 현재 DB 데이터 개요

2026-03-30 기준 실행 중인 `agribot_db`의 주요 row 수:

| 테이블 | 건수 |
| --- | --- |
| `zones` | 1 |
| `robots` | 1 |
| `plants` | 24 |
| `fruits` | 24 |
| `missions` | 4 |
| `crop_observations` | 24 |
| `iot_devices` | 8 |
| `actuation_commands` | 3 |
| `actuation_logs` | 3 |
| `alerts` | 3 |
| `environment_samples` | 6 |
| `harvest_events` | 3 |
| `ai_judgments` | 72 |

AI 판단 분포 요약:

- `DISEASE`: `normal` 21건, `powdery_mildew` 1건, `gray_mold` 1건, `calcium_deficiency` 1건
- `RIPENESS`: `ripe` 4건, `turning` 20건
- `HARVEST_DECISION`: `harvest_candidate` 4건, `harvest_hold` 2건, `reobserve` 18건

## 7. DB 접속 정보 / ERD / 주요 계정 및 프로퍼티 정의 파일 목록

| 파일 | 역할 | 주요 내용 |
| --- | --- | --- |
| `backend/database.py` | SQLAlchemy 접속 정의 | `AGRIBOT_DATABASE_URL`, 기본 DSN |
| `backend/docker-compose.yml` | DB/브로커/관리도구 실행 정의 | `POSTGRES_USER`, `POSTGRES_PASSWORD`, `POSTGRES_DB`, `PGADMIN_DEFAULT_EMAIL`, `PGADMIN_DEFAULT_PASSWORD` |
| `backend/pgadmin/servers.json` | pgAdmin 서버 등록 정보 | Host=`postgres`, DB=`agribot_db`, Username=`agribot` |
| `backend/models.py` | 실제 ORM 모델 정의 | `zones`, `robots`, `plants`, `fruits`, `missions`, `harvest_events`, `ai_judgments` 포함 13개 테이블 |
| `backend/sql/ai_judgments.sql` | AI 판단 전용 DDL | `ai_judgments` 테이블 및 index 정의 |
| `backend/seed_demo_data.py` | 데모 데이터 적재 스크립트 | 고정 Robot/Mission ID, 병해/숙도/수확결정 샘플 데이터 |
| `backend/robot_map_service.py` | DB seed 및 지도/작물/IoT 참조 로직 | `crop_instances.yaml`, `farm_world.sdf`, `iot_devices.yaml` 참조 |
| `backend/routers/inference.py` | AI 판단/데모 추론 API 라우터 | `/api/v1/inference/demo/confirm`, `/judgments/latest`, `/harvest/individual`, `/harvest/bulk` |
| `backend/services/ai_judgments/policy.py` | AI 판단 규칙/해석 정책 | 질병, 숙도, 수확결정 canonical code 및 action code 매핑 |
| `backend/services/ai_judgments/service.py` | AI 판단 저장/조회 서비스 | 최신 판단 조회, 히스토리 조회, 개별/일괄 수확 의사결정 생성 |
| `backend/services/ai_judgments/schemas.py` | AI 판단 API 스키마 | `DISEASE`, `RIPENESS`, `HARVEST_DECISION` 응답/요청 모델 |
| `docs/DB_SCHEMA_초안.sql` | PostgreSQL 스키마 초안 | ENUM, TABLE, FK, INDEX 정의 |
| `docs/ERD_구조_예시.svg` | ERD 시각 자료 | 테이블 관계도 |
| `artifacts/demo_inputs/diagnosis_demo_manifest.json` | 시연용 진단 이미지 매핑 | plant ID별 demo 이미지, fruit ID, preliminary label 정의 |
| `agribot_ws/src/agribot_description/config/crop_instances.yaml` | 식물/토마토 canonical ID 정의 | `plant_id`, `tomato_id`, 위치 정보 |
| `agribot_ws/src/agribot_iot/config/iot_devices.yaml` | IoT 장치 속성 정의 | device ID, topic, MQTT namespace, capability |
| `agribot_ws/src/agribot_description/worlds/farm_world.sdf` | 월드/장치 배치 정의 | 스프링클러 위치, 시뮬레이션 월드 |
| `agribot_ws/src/agribot_navigation/maps/farm_map.yaml` | 정적 맵 메타데이터 | map origin, resolution |

## 8. DB 덤프 파일 최신본

- 파일 경로: `/home/ssafy/Desktop/pjt/S14P21A602/exec/db/agribot_db_dump_20260330.sql`
- 생성 일시: 2026-03-30
- 생성 기준: 실행 중인 Docker 컨테이너 `backend-postgres-1`
- 생성 명령:

```bash
docker exec backend-postgres-1 pg_dump -U agribot -d agribot_db --clean --if-exists --inserts > exec/db/agribot_db_dump_20260330.sql
```

- 비고:
  - plain SQL dump 형식
  - `DROP ... IF EXISTS` + `INSERT` 포함
  - `ai_judgments` 72건과 기존 병해/수확 이력 데이터 포함
  - `backend/seed_demo_data.py` 적용 직후 기준 최신본
  - `agribot_dump_verify_20260330` 임시 DB에 복원 검증 완료

## 9. 시연 시나리오

### 9.1 시연 전 준비

터미널별 실행 순서:

1. Backend

```bash
cd /home/ssafy/Desktop/pjt/S14P21A602
source <(./scripts/operator_runtime_health.sh --print-env)
./scripts/operator_runtime_health.sh --reset-runtime
./scripts/backend_up.sh
```

2. Frontend

```bash
cd /home/ssafy/Desktop/pjt/S14P21A602
./scripts/frontend_up.sh
```

3. ROS 시뮬레이션

```bash
cd /home/ssafy/Desktop/pjt/S14P21A602
./scripts/agribot_launch.sh agribot_bringup simulation.launch.py use_iot:=true
```

헤드리스/원격 검증 환경 권장:

```bash
./scripts/agribot_launch.sh agribot_bringup simulation.launch.py use_iot:=true use_rviz:=false
```

4. Mission Manager

```bash
cd /home/ssafy/Desktop/pjt/S14P21A602
./scripts/mission_manager_up.sh
```

5. Harvest Action Server

```bash
cd /home/ssafy/Desktop/pjt/S14P21A602
./scripts/harvest_action_server_up.sh
```

상태 점검:

```bash
./scripts/operator_runtime_health.sh
```

### 9.2 시연 포인트 1: 메인 화면 구동 확인

- 브라우저 접속: `http://localhost:5173`
- 확인 항목:
  - 메인 대시보드 로딩
  - 로봇 상태 카드 노출
  - API 응답 정상

검증 명령:

```bash
curl -s http://localhost:8000/api/v1/robot/status | jq
curl -s http://localhost:8000/api/v1/dashboard/summary | jq
```

### 9.3 시연 포인트 2: 병해 알림 조회

현재 DB 기준 병해 알림 대상:

- `farm01_plant_06`: `tomato_powdery_mildew`
- `farm01_plant_15`: `tomato_gray_mold`
- `farm01_plant_22`: `tomato_blossom_end_rot`

브라우저 동선:

1. `/alerts` 또는 `/plants` 화면으로 이동
2. 병해 알림 카드 확인
3. 식물 상세에서 최근 관측 이미지와 최신 AI 판단 확인

검증 명령:

```bash
curl -s http://localhost:8000/api/v1/alerts | jq '.data[:3]'
curl -s http://localhost:8000/api/v1/plants/farm01_plant_06/observations | jq
curl -s "http://localhost:8000/api/v1/inference/judgments/latest?plant_id=farm01_plant_06&judgment_type=DISEASE" | jq
```

기대 결과:

- 병해 라벨, confidence, 이미지, 추천 액션이 노출됨
- 심각도(`CRITICAL`/`WARNING`)가 함께 보임
- `DISEASE` judgment의 `canonical_code`, `risk_level`, `recommended_action_code`가 DB와 일치함

### 9.4 시연 포인트 3: 메인 화면에서 `진단하기`

권장 시연 대상:

- `farm01_plant_18`
- 프론트엔드 `farm-command-page` 기준 `DEMO_DIAGNOSIS_PLANT_ID`로 매핑된 식물이며, 관측 위치 도착 후 `/api/v1/inference/demo/confirm` 호출까지 이어진다.

브라우저 동선:

1. 메인 화면에서 `farm01_plant_18` 식물 선택
2. 식물 모달에서 `진단하기` 클릭
3. 버튼 상태가 `진단 이동 준비 -> 이동 중 -> 도착 완료`로 바뀌는지 확인
4. 진단 완료 후 AI 판단 이력이 갱신되는지 확인

검증 명령:

```bash
cat "$AGRIBOT_RUNTIME_DIR/robot_manual_command.json" | jq
cat "$AGRIBOT_RUNTIME_DIR/robot_manual_command_status.json" | jq
curl -s http://localhost:8000/api/v1/robot/commands/latest | jq
curl -s "http://localhost:8000/api/v1/inference/judgments/history?plant_id=farm01_plant_18&limit=5" | jq
```

기대 결과:

- `command_type`이 `navigate_to_pose`
- status가 `pending -> running -> succeeded` 순으로 변경
- 프론트 상태 문구가 runtime 상태와 동기화됨
- `farm01_plant_18` 기준 시연용 이미지가 backend에 전달되고, 저장 후 `DISEASE`, `RIPENESS`, `HARVEST_DECISION` 판단 이력을 한 대상에서 함께 확인할 수 있음

### 9.5 시연 포인트 4: AI 판단 기반 수확 의사결정 확인

현재 DB 기준 `HARVEST_DECISION` 분포:

- 수확 후보(`harvest_candidate`): `farm01_plant_02_tomato_01`, `farm01_plant_04_tomato_01`, `farm01_plant_11_tomato_01`, `farm01_plant_12_tomato_01`
- 수확 보류(`harvest_hold`): `farm01_plant_06_tomato_01`, `farm01_plant_15_tomato_01`
- 재관측(`reobserve`): 나머지 18개 fruit

브라우저 동선:

1. `/plants` 또는 메인 화면에서 수확 후보 식물과 보류 식물을 각각 선택
2. 상세 카드에서 최근 판단 라벨을 비교
3. 수확 후보는 `수확 가능`, 보류 대상은 `재진단/보류` 성격으로 표시되는지 확인

검증 명령:

```bash
curl -s "http://localhost:8000/api/v1/inference/judgments/latest?fruit_id=farm01_plant_02_tomato_01&judgment_type=HARVEST_DECISION" | jq
curl -s "http://localhost:8000/api/v1/inference/judgments/latest?fruit_id=farm01_plant_06_tomato_01&judgment_type=HARVEST_DECISION" | jq
curl -s "http://localhost:8000/api/v1/inference/judgments/history?fruit_id=farm01_plant_02_tomato_01&limit=5" | jq
```

기대 결과:

- 수확 후보 fruit는 `canonical_code=harvest_candidate`, `recommended_action_code=HARVEST`
- 병해 개체는 `canonical_code=harvest_hold` 또는 `reobserve`로 분리됨
- 한 fruit 기준으로 `DISEASE -> RIPENESS -> HARVEST_DECISION` 순의 판단 이력을 추적할 수 있음

### 9.6 시연 포인트 5: 메인 화면에서 `수확하기`

현재 DB 기준 수확 가능 fruit:

- `farm01_plant_02_tomato_01`
- `farm01_plant_04_tomato_01`
- `farm01_plant_11_tomato_01`
- `farm01_plant_12_tomato_01`

브라우저 동선:

1. 메인 화면에서 수확 가능 식물 선택
2. 모달에서 `수확하기` 클릭
3. 상태가 `접근 중 -> 토마토 집는 중 -> 바구니 적재 중 -> 완료`로 변하는지 확인
4. `/harvest` 화면에서 통계 갱신 확인

검증 명령:

```bash
cat "$AGRIBOT_RUNTIME_DIR/robot_mission_request.json" | jq
MISSION_ID="$(jq -r '.mission_id' "$AGRIBOT_RUNTIME_DIR/robot_mission_request.json")"
curl -s "http://localhost:8000/api/v1/missions/$MISSION_ID" | jq
curl -s http://localhost:8000/api/v1/harvests/stats | jq
cat "$AGRIBOT_RUNTIME_DIR/harvest_action_status.json" | jq
cat "$AGRIBOT_RUNTIME_DIR/harvest_basket_state.json" | jq
cat "$AGRIBOT_RUNTIME_DIR/harvest_latest_event.json" | jq
```

기대 결과:

- `request_type`이 `harvest_target`
- `plant_id`, `fruit_id`가 명확히 기록됨
- 수확 완료 후 basket count 및 latest harvested fruit 정보 갱신

### 9.7 시연 포인트 6: `패트롤로 병 진단`

브라우저 동선:

1. 메인 화면 오른쪽 조작판에서 `패트롤로 병 진단` 클릭
2. 패트롤 미션이 accepted 후 polling 기반으로 상태 갱신되는지 확인

검증 명령:

```bash
cat "$AGRIBOT_RUNTIME_DIR/robot_mission_request.json" | jq
MISSION_ID="$(jq -r '.mission_id' "$AGRIBOT_RUNTIME_DIR/robot_mission_request.json")"
curl -s "http://localhost:8000/api/v1/missions/$MISSION_ID" | jq
cat "$AGRIBOT_RUNTIME_DIR/robot_mission_status.json" | jq
```

기대 결과:

- `request_type` = `start_patrol`
- `patrol_mode` = `diagnosis`
- 상태가 `pending -> running -> succeeded/failed`로 변함

### 9.8 시연 포인트 7: `패트롤로 전체 수확`

브라우저 동선:

1. 메인 화면 오른쪽 조작판에서 `패트롤로 전체 수확` 클릭
2. 수확 패트롤 미션 상태와 `/harvest` 화면 통계가 연동되는지 확인
3. 필요하면 AI 판단 endpoint로 현재 수확 후보 대상이 반영되는지 추가 확인

검증 명령:

```bash
cat "$AGRIBOT_RUNTIME_DIR/robot_mission_request.json" | jq
MISSION_ID="$(jq -r '.mission_id' "$AGRIBOT_RUNTIME_DIR/robot_mission_request.json")"
curl -s "http://localhost:8000/api/v1/missions/$MISSION_ID" | jq
curl -s http://localhost:8000/api/v1/harvests | jq '.data[:5]'
curl -s "http://localhost:8000/api/v1/inference/judgments/history?judgment_type=HARVEST_DECISION&plant_id=farm01_plant_02&limit=3" | jq
```

기대 결과:

- `request_type` = `start_patrol`
- `patrol_mode` = `harvest`
- 수확 이력과 상태 카드가 함께 갱신됨

## 10. 검증 결과

### 10.1 DB 검증

- `backend/seed_demo_data.py`를 2026-03-30에 다시 적용해 기준 데이터를 정리했다.
- `public` 스키마 기준 테이블 13개가 존재함을 확인했다.
- 주요 row 수:
  - `zones` 1
  - `robots` 1
  - `plants` 24
  - `fruits` 24
  - `missions` 4
  - `crop_observations` 24
  - `iot_devices` 8
  - `actuation_commands` 3
  - `actuation_logs` 3
  - `alerts` 3
  - `environment_samples` 6
  - `harvest_events` 3
  - `ai_judgments` 72
- `ai_judgments` 분포:
  - `DISEASE`: `normal` 21, `powdery_mildew` 1, `gray_mold` 1, `calcium_deficiency` 1
  - `RIPENESS`: `ripe` 4, `turning` 20
  - `HARVEST_DECISION`: `harvest_candidate` 4, `harvest_hold` 2, `reobserve` 18
- dump 파일 `exec/db/agribot_db_dump_20260330.sql`을 임시 DB `agribot_dump_verify_20260330`에 실제로 복원했고, `missions` 4건, `ai_judgments` 72건이 원본 DB와 동일함을 확인했다.

### 10.2 테스트 검증

- Backend pytest: `62 passed`
- ROS bringup/navigation pytest: `168 passed`

### 10.3 매뉴얼 실행 검증

2026-03-30 기준 실제 실행 확인 항목:

1. `source <(./scripts/operator_runtime_health.sh --print-env)` 및 `./scripts/operator_runtime_health.sh --reset-runtime` 성공
2. `./scripts/backend_up.sh` 성공
3. `./scripts/frontend_up.sh` 성공
4. `./scripts/agribot_launch.sh agribot_bringup simulation.launch.py use_iot:=true use_rviz:=false` 성공
5. `./scripts/mission_manager_up.sh` 성공
6. `./scripts/harvest_action_server_up.sh` 성공
7. API 확인 성공:
   - `GET /api/v1/dashboard/summary`
   - `GET /api/v1/robot/status`
   - `GET /api/v1/harvests/stats`
   - `GET /api/v1/inference/judgments/latest?fruit_id=farm01_plant_02_tomato_01&judgment_type=HARVEST_DECISION`
8. Frontend 실사용 검증 성공:
   - `진단하기` 실행 후 `/api/v1/robot/commands/latest` 기준 `status=succeeded`
   - `수확하기` 실행 후 `/api/v1/harvests/stats` 기준 `mission_status=succeeded`, `harvested_count=1`, `basket_count=1`

실제 확인된 응답 예시 요약:

- 대시보드 요약: `active_task=PATROL · RUNNING`, `critical_alert_count=3`, `ready_fruit_count=4`
- 로봇 상태: `pose_source=live`, `current_control_state.available=true`
- AI 판단 조회: `farm01_plant_02_tomato_01`의 최신 `HARVEST_DECISION`이 `harvest_candidate / HARVEST`
- 최신 수동 이동 상태: `status=succeeded`, 메시지=`울타리 쪽으로 갈 수 있는 최대 근접 관측 위치까지 이동을 완료했습니다.`
- 최신 수확 상태: `mission_status=succeeded`, `current_phase=COMPLETED`, `last_harvested_fruit_id=farm01_plant_11_tomato_01`

주의 사항:

- `./scripts/operator_runtime_health.sh`는 clean reset 직후 첫 수동 이동 전에는 `robot_manual_command_status.json`이 없다고 보고할 수 있다.
- 이는 초기 상태에서는 아직 수동 명령 이력이 없기 때문이며, `진단하기` 또는 수동 이동 명령을 1회 실행하면 status 파일이 생성되고 이후 정상 추적된다.

## 11. 참고 파일

- `README.md`
- `frontend/README.md`
- `docs/README.md`
- `docs/README_LAUNCH.md`
- `docs/프론트엔드 켜놓은 후 시연용 명령어 모음.md`
- `docs/운영자_미션_E2E_검증_체크리스트.md`
