<!--
  Nav2 navigate-through-poses tree tuned for tight-space recovery.
  Turn-around and drive-out are attempted before backing out so the robot
  stays in a forward-driving posture unless the aisle is truly blocked.
-->
<root BTCPP_format="4" main_tree_to_execute="MainTree">
  <BehaviorTree ID="MainTree">
    <RecoveryNode number_of_retries="10" name="NavigateRecovery">
      <PipelineSequence name="NavigateWithReplanning">
        <ControllerSelector
          selected_controller="{selected_controller}"
          default_controller="FollowPath"
          topic_name="controller_selector"/>
        <PlannerSelector
          selected_planner="{selected_planner}"
          default_planner="GridBased"
          topic_name="planner_selector"/>
        <RateController hz="0.333">
          <RecoveryNode number_of_retries="1" name="ComputePathThroughPoses">
            <ReactiveSequence>
              <RemovePassedGoals input_goals="{goals}" output_goals="{goals}" radius="0.7"/>
              <ComputePathThroughPoses
                goals="{goals}"
                path="{path}"
                planner_id="{selected_planner}"
                error_code_id="{compute_path_error_code}"/>
            </ReactiveSequence>
            <Sequence>
              <WouldAPlannerRecoveryHelp error_code="{compute_path_error_code}"/>
              <ClearEntireCostmap
                name="ClearGlobalCostmap-Context"
                service_name="global_costmap/clear_entirely_global_costmap"/>
            </Sequence>
          </RecoveryNode>
        </RateController>
        <RecoveryNode number_of_retries="1" name="FollowPath">
          <FollowPath
            path="{path}"
            controller_id="{selected_controller}"
            error_code_id="{follow_path_error_code}"/>
          <Sequence>
            <WouldAControllerRecoveryHelp error_code="{follow_path_error_code}"/>
            <ClearEntireCostmap
              name="ClearLocalCostmap-Context"
              service_name="local_costmap/clear_entirely_local_costmap"/>
          </Sequence>
        </RecoveryNode>
      </PipelineSequence>
      <Sequence>
        <Fallback>
          <WouldAControllerRecoveryHelp error_code="{follow_path_error_code}"/>
          <WouldAPlannerRecoveryHelp error_code="{compute_path_error_code}"/>
        </Fallback>
        <ReactiveFallback name="RecoveryFallback">
          <GoalUpdated/>
          <RoundRobin name="RecoveryActions">
            <Sequence name="TurnAroundAndDriveOut">
              <ClearEntireCostmap
                name="ClearLocalCostmap-TurnAround"
                service_name="local_costmap/clear_entirely_local_costmap"/>
              <Spin spin_dist="3.14159" error_code_id="{spin_error_code}"/>
              <DriveOnHeading
                dist_to_travel="0.60"
                speed="0.14"
                time_allowance="8.0"
                error_code_id="{drive_on_heading_error_code}"/>
            </Sequence>
            <Spin spin_dist="3.14159" error_code_id="{spin_error_code}"/>
            <Sequence name="ClearAndBackOutShort">
              <ClearEntireCostmap
                name="ClearLocalCostmap-ShortBackout"
                service_name="local_costmap/clear_entirely_local_costmap"/>
              <ClearEntireCostmap
                name="ClearGlobalCostmap-ShortBackout"
                service_name="global_costmap/clear_entirely_global_costmap"/>
              <BackUp
                backup_dist="0.40"
                backup_speed="0.10"
                time_allowance="6.0"
                error_code_id="{backup_error_code}"/>
            </Sequence>
            <Sequence name="BackOutLong">
              <BackUp
                backup_dist="0.90"
                backup_speed="0.08"
                time_allowance="12.0"
                error_code_id="{backup_error_code}"/>
              <ClearEntireCostmap
                name="ClearLocalCostmap-LongBackout"
                service_name="local_costmap/clear_entirely_local_costmap"/>
            </Sequence>
            <Wait wait_duration="2.0"/>
          </RoundRobin>
        </ReactiveFallback>
      </Sequence>
    </RecoveryNode>
  </BehaviorTree>
</root>
